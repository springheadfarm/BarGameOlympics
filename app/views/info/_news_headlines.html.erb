<% @news.each do |article| %>

<div class="news_teaser">
	<h5><%= article.title %></h5>
	<%= stripdown(article.markdown).split.first(20).join(' ') %>... <%= link_to 'more', article %>
</div>

<% end %>